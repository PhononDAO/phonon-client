/**
 * Error Object to be thrown in errors
 */
export interface ErrorObject {
  code: number;
  message: string;
}

/**
 * Chain object
 */
export interface Chain {
  name: string;
  textColor: string;
  bgColor: string;
}
