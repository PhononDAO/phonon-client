/**
 * Error Object to be thrown in errors
 */
export type ErrorObject = {
  code: number;
  message: string;
};

/**
 * Chain object
 */
export type Chain = {
  name: string;
  textColor: string;
  bgColor: string;
};
